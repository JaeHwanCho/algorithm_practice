#include <iostream>
#include <string>

using namespace std;

int num_10926() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	string s;

	cin >> s;

	cout << s << "??!";

	return 0;
}